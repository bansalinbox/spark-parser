package test

import com.typesafe.config.ConfigFactory

/**
 * @author ${user.name}
 */
object App {
  
  
  
  def main(args : Array[String]) {
    
    var text = "gourav,bansal,last,"
    
    println(text.slice(0, text.length()-1))
    
  }

}
